# Development dependencies
# Install: pip install -r requirements-dev.txt
# Note: Runtime dependencies must be installed first: pip install -r requirements.txt

# Debugging tools
debugpy==1.8.20

# Code quality tools (keep in sync with .pre-commit-config.yaml)
ruff==0.15.12
pylint==4.0.5

# Testing tools
pytest==9.0.3
pytest-asyncio==1.3.0

# pre-commit (optional, for git hooks)
pre-commit==4.6.0
